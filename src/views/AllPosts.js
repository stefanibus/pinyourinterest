import React, { useState } from "react";
import { useParam } from "react-router-dom";
const AllPosts = () => {
  // const { Futureuse } = useParams();

  return (
    <div className="allpots">
      <div>irgendwas</div>
      HelloAllPots
    </div>
  );
};
export default AllPosts;
